# identity-api-gateway
Main auth ingress: login, token issuance/verification, session routing.
Owned by @identitysec/backend (security review required on every PR).
Routes authenticated requests to billing-service and internal services.
